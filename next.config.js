/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: undefined,
  images: {
    loader: 'default',
    path: '/next-app'
  }
}

module.exports = nextConfig
