/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: undefined
  images: {
    loader: 'default',
    path: undefined
  }
}

module.exports = nextConfig
